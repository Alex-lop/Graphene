# Goal run — 2026-08-26, the door, the face, and `/graphene`

Session executing `GOAL_DIRECTIVE.md` (2026-08-26) in goal mode with push
authority. One orchestrating session; exploration and verification delegated
to subagents; every commit, gate, and push from the orchestrating session.
Started 04:00 EDT.

## Read this first

Phase A is the door: the README is 87 lines and says what Graphene is in seven
sentences; the proof table moved intact to `docs/PROOF.md` and the command map
to `docs/COMMANDS.md`, with the tests that held README to the CLI now holding
those documents instead. Shadow Agent is deleted in one commit with a reason.
The quickstart was run verbatim, cold, in a fresh `python:3.13-slim` container.
Every connect snippet was checked against the tool's current docs and two
assumptions were wrong (Claude Code defaults to stdio; Gemini exposes MCP
prompts as bare `/<prompt>`). Nothing is labelled proven that was not run.

## State

| | |
|---|---|
| badge on `main` | green at `d7f1732` (run 32844350285) when this run began; see the final state line for this run's pushes |
| local tip / origin tip | see the final state line |
| spend | $0 — nothing live in Phase A |
| secrets | `scripts/secret_scan.py` clean at every commit |
| quiescence | `caffeinate` running for the session; released at stop |

## Change table

| Claim | Commit | Evidence | Verdict |
|---|---|---|---|
| Shadow Agent removed from every user-facing surface, code, tests, fixtures, docs, and the proof contract; zero references outside `docs/reports/` and `evidence/` | `e2e10ea` | `git grep -n -i -E "shadow agent\|graphene shadow\|graphene\.shadow\|shadow_agent\|shadow\.event\|SHADOW\|shadow\b" -- . ':!evidence' ':!docs/reports'` returns only CSS `box-shadow`/`drop-shadow` properties; ruff clean; `test_main`, `test_readme_contract`, `test_documentation_truth`, `test_capsule_cli`, `test_graph_economics_deferral`, `test_ci_contract` green | **LOCAL** — full gate before the push |
| README rewritten for a newcomer, ≤120 lines, quickstart verified cold, three connect blocks verified against current docs, benchmark placeholders with zero numbers, four-row proof summary, Shadow nowhere | (this phase's second commit) | `wc -l README.md` = 87; `test_readme_contract.py` now asserts `len(readme.splitlines()) <= 120`; cold container log: `uv sync --frozen` 2 s, `graphene mission replay taskmaster` printed its truth label and served Mission Control until the 60 s timeout (exit 124: a container has no browser, the command does not exit on its own); `test_graph_economics_deferral.py` and `test_documentation_truth.py` green unmodified | **LOCAL** |

## Overrides taken, and why

1. **CP-A2 landed before CP-A1.** `test_readme_contract.py` requires every registered command to be documented, so a README with "Shadow nowhere" is only green once the `shadow` command is gone. The deletion commit came first; both are Phase A commits behind the same gate.
2. **`test_readme_contract.py` and `test_capsule_cli.py` follow the content they check.** The directive freezes `test_graph_economics_deferral.py` and `test_documentation_truth.py` (unchanged); it moves the proof table to `docs/PROOF.md`, so the assertions that pinned proof rows and the command map to `README.md` now pin them to `docs/PROOF.md` and `docs/COMMANDS.md`. No assertion was dropped; one was added (the 120-line cap).
3. **The safety-boundary sentences moved into `docs/TASKMASTER_PRODUCT_CONTRACT.md`**, not deleted: the frozen doc-truth test requires them in one of four canonical documents, and the product contract is where an authority boundary belongs.
4. **`docs/DEMO_GUIDE.md` is not deleted** although the directive names it as a candidate: `test_graph_economics_deferral.py::test_no_public_surface_claims_a_measured_economic_advantage` asserts the file exists, and that test is frozen by the same directive. Rule 3 ("never delete anything a green CI job consumes") decides it.
5. **Connect blocks carry a date, not a claim.** `graphene-mcp` today needs `GRAPHENE_LINEAGE_DB` and a `--task`; the bare launch form, `.mcp.json`, and the `goal` prompt are Phase C. The README says so beside each block rather than describing a surface that does not exist yet.

## Not done, honestly

- Phase B and Phase C: not started at the time of this section.
- `docs/assets/ui-terminal.png` and the three benchmark frames are labelled placeholder images; the link test requires the files to exist, so each one says "PLACEHOLDER" and "no number is claimed" in the image itself.

## Systemic findings

1. **A required link and a frozen link test force a placeholder file.** The directive asks for an image link before the image exists while freezing the test that resolves every link; the honest resolution is a file that says it is a placeholder, not a missing file.

## Final state

(written at stop)
