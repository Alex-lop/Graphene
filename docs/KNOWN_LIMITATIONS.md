# Known limitations

A mechanism test is not external proof. These limits describe the current
recovery implementation and stay open until the matching evidence exists.

| Area | Current status | What closes it |
|---|---|---|
| Current live Gemini | `NOT PROVEN`. The code pins `google-adk==2.5.0` and requests `gemini-3.5-flash`, but no credentialed current-tree Orders mission has captured returned identity and reached `completed` | Revalidate eligibility/catalog, then complete the runbook with sanitized receipts |
| Real model-child death | `NOT PROVEN`. The framed child, dispatch barrier, owned identity, interruption receipt, and selective retry are locally tested; no real Gemini child has been killed | Barrier-acknowledged live kill, preserved sibling, higher-fence retry, exact result, and completion |
| Detached live mission | Scripted process tests prove controller disconnect and fresh-client reattachment. No live provider mission has demonstrated this | Start through the intended controller, disconnect, reattach, and complete live |
| Codex controller | Config example only; current Codex MCP start/disconnect/reattach flow has not been driven | Captured current-SHA Codex run using the installed entry point |
| Human identity | MCP review approval is `server_derived` relay evidence, not authenticated human attestation | An explicitly authenticated human approval surface, if required |
| Policy pre-authorization | Locally tested for deterministic policy decisions. The checked-in policy is intentionally narrow and does not make arbitrary repositories safe | More repository/policy matrices; keep default-deny behavior |
| Orders hero | Target, policy, and immutable suite are tested. No real model has completed the migration on this runtime | Full live proof from clean base to isolated result and `why` |
| Exact-SHA packaging | The verifier and clean/expected-SHA/canonical-remote-ref gate are checked in. Run-specific truth lives in the SHA-named external manifest or CI result, never in the commit it proves | Retain the external manifest or CI result for the exact implementation SHA |
| Aggregate stability | Focused checks do not replace a frozen full-matrix/soak result. Historical SQLite hang evidence neither proves recurrence nor proves closure for a later SHA | Run the full bounded matrix repeatedly with hang diagnostics and bind results to the implementation SHA |
| Installed live demo | `graphene demo --live` resolves the packaged North Star materializer/resources in installed artifacts, but remains the legacy watcher/edit/review flow rather than the detached MCP hero; no current live provider run is proven | Capture a current installed live run only if this legacy path is a judging requirement |
| Runtime pytest | Not installed. Legacy/scripted paths that execute the fixture test command need a development/test environment | Keep pytest out of runtime dependencies; provide an explicit test executor/image |
| Platform | The host-sandbox check executor requires macOS `/usr/bin/sandbox-exec`. Linux proves replay and fail-closed boundaries, not that live check executor | Responsive Docker executor or another reviewed isolation boundary |
| Docker | No responsive-daemon smoke for the current tree | Immutable image build and opt-in smoke |
| Google Cloud | Cloud Run and real Firestore are not deployed or authenticated as proof | Owner-selected project, bounded deployment, authenticated durability smoke, and sanitized evidence |
| Firestore parity | Official-emulator paths do not establish complete SQLite scheduler parity or cloud durability | Shared transition corpus plus live authenticated proof |
| Cloud check authority | Remote results are executor-attested; the coordinator verifies bindings but does not rerun the check | Server-minted deterministic check authority, if required |
| Replanning | A request can pause work, and a user can export/edit/revise/lint/diff/approve. No model-generated replacement revision exists | A bounded linked revision workflow with explicit authority |
| Planner crash recovery | Planning now runs in an isolated framed child with a durable transport acknowledgement, strong process identity, and at most two attempts. Pre/post-dispatch crash windows are locally tested, but no live provider planner has been killed | Capture a current-SHA live planner interruption and its truthful provider/billing outcome |
| Ordered write transfer | Overlapping work write paths are refused even when dependency order could make transfer safe | Explicit ownership-transfer contract and adversarial tests |
| Artifact closure | V2 envelopes bind direct accepted inputs; transitive snapshot/delta subsumption is unsupported | Add only for a real plan requiring non-flat ancestry |
| Repository breadth | Regular-file create/update/delete/rename/chmod is bounded; symlink/submodule mutation fails closed | Published repository/runtime matrix plus live proof |
| Cancellation/why | A task with no publication has no file-rooted `why PATH` route to its cancelled attempt | Attempt-rooted query shape and live cancellation proof |
| Retention | Policy metadata exists; automatic expiry/purge does not | Tested purge implementation and operator controls |
| Capsules | Cold verification proves internal consistency, not producer authenticity, provider truth, or host-clock accuracy | External signature/anchor only after defining a trust model |
| UI and media | Replay and read-only UI are local fixture proof. Current hero screenshot, GIF, and film do not exist | Capture only after the full live run is green |
| Benchmark | Harness mechanics are not a cost, speed, quality, or reliability result | Repeated equal-gate runs with raw receipts, median, and P95 |

Historical 2026-08-23 live evidence remains evidence for the historical source
that produced it. It does not close any current-runtime row above.

The legacy Auth protocol tour remains a supported regression surface, not
Taskmaster proof. Current mission-plan validation rejects its reserved
`legacy_auth_v2` attempt link.
